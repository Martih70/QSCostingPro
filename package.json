{
  "name": "khconstruct",
  "version": "1.0.0",
  "description": "Cost forecasting application for Kingdom Hall renovations and maintenance",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "git-diagnostic": "node /Users/martinhamp/herd/.diagnostic-tools/git-diagnostic.mjs",
    "forge-diagnostic": "node /Users/martinhamp/herd/.diagnostic-tools/forge-diagnostic.mjs",
    "type-check": "npm run type-check --workspace=client && npm run type-check --workspace=server"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
